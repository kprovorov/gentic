{
  "name": "@gentic/gentic",
  "version": "0.0.1",
  "description": "Gentic agent: runs Claude Code over the Agent Client Protocol for queued issues.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.55.0",
    "@agentclientprotocol/sdk": "^1.1.0",
    "dotenv": "^16.4.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@gentic/eslint-config": "workspace:*",
    "@gentic/typescript-config": "workspace:*",
    "@types/node": "^20",
    "eslint": "^9",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
